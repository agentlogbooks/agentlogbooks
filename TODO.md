# TODO

## OG image (deferred from Task 23)
- All pages currently point `og:image` at `/logo-128.png` (128x128).
- Replace with a 1200x630 PNG rendered with the logo + wordmark "agentlogbooks" + tagline "Structured state for agent skills." on the existing palette.
- Update all pages' `og:image` references in `src/pages/` and rebuild.
